# Reminder Video Folder

Put your own local reminder videos in this folder.

Supported formats:

```text
mp4, mov, m4v
```

The app scans this folder first at reminder time. This repository intentionally keeps the folder empty except for this placeholder, so private video files stay on the user's own Mac.
